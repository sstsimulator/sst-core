SUBDIRS = \
	elements \
	core 

sstdir = $(includedir)/sst
nodist_sst_HEADERS = \
	sst_config.h \
	sst_stdint.h

pkgconfigdir = $(libdir)/pkgconfig
pkgconfig_DATA = SST-${PACKAGE_VERSION}.pc

EXTRA_DIST = mainpage.dox

DISTCLEANFILES = sst_stdint.h
