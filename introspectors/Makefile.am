# -*- Makefile -*-
#
#

DIST_SUBDIRS = $(SST_DIST_INTROSPECTORS)
#SUBDIRS = $(SST_ACTIVE_INTROSPECTORS)

#Manually include subdirectories. This is temporary
#until the SST_ACTIVE_INTROSPECTORS problem is solved 
SUBDIRS = introspector_cpu introspector_cpuTemperature


EXTRA_DIST = 
